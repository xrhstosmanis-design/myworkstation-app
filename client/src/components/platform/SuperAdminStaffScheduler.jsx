import React,{useMemo,useState} from "react";
import {CalendarClock,Printer,Send,X} from "lucide-react";
export default function SuperAdminStaffScheduler({company,store,request,onClose}){const[weekStart,setWeekStart]=useState(""),[schedule,setSchedule]=useState(null),[warnings,setWarnings]=useState([]),[busy,setBusy]=useState(""),[error,setError]=useState("");const days=useMemo(()=>{const result={};for(const row of schedule?.assignments||[]){const key=row.date.slice(0,10);(result[key]??=[]).push(row)}return result},[schedule]);const generate=async()=>{setBusy("generate");setError("");try{const result=await request("/api/schedules/generate",{method:"POST",body:JSON.stringify({storeId:store.id,...(weekStart?{weekStart}: {})})});setSchedule(result.schedule);setWarnings(result.warnings||[])}catch(e){setError(e.message)}finally{setBusy("")}};const email=async()=>{setBusy("email");setError("");try{const result=await request(`/api/schedules/${schedule.id}/email`,{method:"POST",body:"{}"});alert(`Στάλθηκε σε ${result.recipients.length} εργαζομένους.`)}catch(e){setError(e.message)}finally{setBusy("")}};return <div className="platform-modal staff-scheduler-modal"><section className="platform-security-dialog"><button className="modal-close" onClick={onClose}><X/></button><h2><CalendarClock/> Πρόγραμμα Εργαζομένων με AI</h2><p>{company.name} · <b>{store.name}</b> · Πρόσβαση Super Admin πάντα ενεργή</p><div className="platform-title-actions"><input type="date" value={weekStart} onChange={e=>setWeekStart(e.target.value)}/><button onClick={generate} disabled={Boolean(busy)}>{busy==="generate"?"Δημιουργία…":"Δημιουργία προγράμματος"}</button>{schedule&&<button className="secondary" onClick={()=>window.print()}><Printer/> PDF / Εκτύπωση</button>}{schedule&&<button className="secondary" onClick={email} disabled={Boolean(busy)}><Send/> Email εργαζομένων</button>}</div>{error&&<div className="platform-alert error">{error}</div>}{warnings.length>0&&<div className="platform-alert error">{warnings.length} ακάλυπτες ή προβληματικές θέσεις.</div>}{!schedule?<div className="platform-empty">Θα χρησιμοποιηθούν μόνο οι εργαζόμενοι, οι άδειες, οι διαθεσιμότητες και οι βάρδιες του «{store.name}».</div>:<div className="schedule-grid super-admin-schedule-grid">{Object.entries(days).map(([date,rows])=><article className="day" key={date}><h3>{new Date(`${date}T12:00:00`).toLocaleDateString("el-GR",{weekday:"long",day:"2-digit",month:"2-digit"})}</h3>{rows.map(row=><div className={`assignment ${!row.employee?"uncovered":""}`} key={row.id}><b>{row.shiftType.name} · {row.shiftType.startTime}-{row.shiftType.endTime}</b><span>{row.employee?.fullName||"ΑΚΑΛΥΠΤΟ"}</span></div>)}</article>)}</div>}</section></div>}
