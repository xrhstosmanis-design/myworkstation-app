import React,{useEffect,useState} from "react";
import {CheckCircle2,RefreshCw,X} from "lucide-react";
const eur=v=>Number(v||0).toLocaleString("el-GR",{style:"currency",currency:"EUR"});
export default function BankLedgerReviewCenter({request,onClose,setMessage}){
 const [items,setItems]=useState([]),[summary,setSummary]=useState({items:[],totals:{availableBalance:0,pendingAmount:0}}),[busy,setBusy]=useState(""),[error,setError]=useState("");
 const load=async()=>{setBusy("load");try{const [review,report]=await Promise.all([request("/api/transactions/bank-ledger/review"),request("/api/transactions/bank-ledger/summary")]);setItems(review.items||[]);setSummary(report||{items:[],totals:{availableBalance:0,pendingAmount:0}})}catch(e){setError(e.message)}finally{setBusy("")}};
 useEffect(()=>{load()},[]); const review=async(item,status)=>{const note=window.prompt(status==="CONFIRMED"?"Σημείωση επιβεβαίωσης":"Αιτιολογία απόκλισης/ακύρωσης");if(!note||note.trim().length<3)return;setBusy(item.id);try{await request(`/api/transactions/bank-ledger/${item.id}/review`,{method:"POST",body:JSON.stringify({status,note:note.trim()})});setItems(rows=>rows.filter(row=>row.id!==item.id));setMessage?.("Η τραπεζική κίνηση ενημερώθηκε.")}catch(e){setError(e.message)}finally{setBusy("")}};
 return <div className="platform-modal"><section className="sa-modal"><header><div><span>SUPER ADMIN ONLY</span><h2>Έλεγχος Ταμείου Τράπεζας</h2><p>Η επιβεβαίωση ενημερώνει μόνο το τραπεζικό ledger. Το ταμείο βάρδιας δεν αλλάζει δεύτερη φορά.</p></div><button className="sa-close" onClick={onClose}><X/></button></header>{error&&<div className="platform-alert error">{error}</div>}<section className="platform-panel" style={{marginBottom:14}}><b>Εικονικό Ταμείο Τράπεζας</b><p>Υπάρχει ένα ενιαίο λογιστικό ταμείο ανά κατάστημα. Δεν συνδέεται με τράπεζα, IBAN ή online banking.</p></section><section className="platform-panel" style={{marginBottom:14}}><b>Σύνοψη Ταμείου Τράπεζας</b><p>Επιβεβαιωμένο: {eur(summary.totals?.availableBalance)} · Σε αναμονή: {eur(summary.totals?.pendingAmount)}</p>{(summary.items||[]).map(row=><small key={row.bankAccountId} style={{display:"block"}}>{row.companyName} · {row.storeName} · {row.bankName} / {row.accountName}: {eur(row.availableBalance)} επιβεβαιωμένα · {eur(row.pendingAmount)} σε αναμονή</small>)}</section><div className="sa-actions"><button onClick={load} disabled={busy==="load"}><RefreshCw/>Ανανέωση</button></div>{items.length?items.map(item=><article key={item.id} className="platform-panel" style={{marginBottom:10}}><b>{item.companyName} · {item.storeName}</b><p>{item.bankName} · {item.accountName} · {item.type} · {eur(item.amount)}</p><small>{new Date(item.occurredAt).toLocaleString("el-GR")} · {item.createdByName} · {item.attachmentFilename||"Χωρίς απόδειξη"}</small><div className="sa-actions"><button disabled={busy===item.id} onClick={()=>review(item,"CONFIRMED")}><CheckCircle2/>Επιβεβαίωση</button><button className="secondary" disabled={busy===item.id} onClick={()=>review(item,"DISCREPANCY")}>Απόκλιση</button></div></article>):<div className="platform-empty">Δεν υπάρχουν τραπεζικές κινήσεις για έλεγχο.</div>}</section></div>;
}