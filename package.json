{
  "name": "myworkstation-app",
  "version": "0.6.0",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "node server/scripts/patch-health-revision-source.js && node server/scripts/patch-kiosk-modifier-only-source.js && node server/scripts/patch-invoice-learning-product-search-route.js && node client/scripts/restore-pos-modifier-groups.js && node client/scripts/patch-close-shift-client-permission.js && node client/scripts/patch-kat-offline-catalog.js && npm run build -w client && npm run prisma:generate -w server",
    "start": "npm run start -w server",
    "seed": "npm run seed -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
