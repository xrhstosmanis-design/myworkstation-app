# Single operator / single active terminal

A Store Mode login revokes any existing active session for the same operator in the same store before issuing a new session. This prevents simultaneous use of two POS terminals by one operator.
