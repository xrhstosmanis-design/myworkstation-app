# MANTZILAS 12798 / 12718 verified 12-piece cartons — 2026-09-18

## LAB evidence

- Status: **LAB FAIL**.
- Invoice `12674` currently exposes supplier codes `12798` and `12718` as `24` stock pieces.
- The operator physically confirmed both cartons contain `12` pieces.
- The current invoice image independently prints `(10+2)` in the `12798` description.
- Both rows also contain printed `19%` discounts which must be recovered by the separate complete-row reread correction; packaging must not change those economics.

## Bounded correction

- Apply a factor of `12` only inside the MANTZILAS packaging helper when the current row has exact normalized supplier code `12798` or `12718`, a LOUX 330 ml description and positive current-invoice package quantity/price.
- Keep invoice quantity and package unit price from the current source row.
- Set only the stock conversion metadata: package unit, `stockUnitsPerInvoiceUnit=12`, stock quantity `invoiceQuantity × 12`, and current package price divided by 12 for the piece-price evidence.
- Preserve net, gross, VAT, discounts, excise and every other current-invoice economic field unchanged.
- An unrelated LOUX 330 ml supplier code remains on the existing generic 24-piece rule.

## Protected behavior

- Preserve all existing MANTZILAS water, bottle, 4-pack, 6-pack, `00009`, `02410` and explicit `12 TMX` rules.
- Preserve the same invoice `12674` source image, AI job, credit intent and unapproved draft.
- No payment, credit, upload, duplicate draft, approval, finalization, stock posting, fiscal, accounting, myDATA or historical-invoice economics mutation.

## Verification and acceptance

- Focused packaging/column regressions: `25/25 PASS`.
- Syntax and diff checks: PASS.
- Full server suite: `1304/1304 PASS`.
- Production client/server/Prisma build: PASS.
- Green CI, merge and exact deployed revision remain required.
- LAB acceptance requires the corrected existing draft to show `12798 = 12` stock pieces and `12718 = 12` stock pieces while retaining their newly reread printed discounts and totals.
