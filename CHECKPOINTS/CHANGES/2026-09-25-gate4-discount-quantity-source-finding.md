# Gate 4 — έκπτωση σε ποσότητα πάνω από ένα, εύρημα κώδικα 25/09/2026

## Νεότερο πραγματικό LAB — PASS οθόνης καλαθιού 25/09 14:34 Ελλάδας

Η διόρθωση του κώδικα συγχωνεύτηκε στο PR #1206 (`64cfd516`), ενώ το αριθμητικό test `server/test/kat-audience-pos-discounts-v1.test.js` έδωσε 5/5 PASS για τη σχετική ροή. Το `/api/health` μετά την εικόνα έδειξε `1615690e990eb566f48b6455c586f2a46e564751`· η ακριβής revision της φυσικής εγκατάστασης τη στιγμή της εικόνας δεν είναι ορατή.

Η εικόνα ιδιοκτήτη `image(20260925-113433).png` της φυσικής συσκευής `LAB POS 2` δείχνει κατάστημα ΕΡΓΑΣΤΗΡΙΟ ΔΟΚΙΜΩΝ, χειριστή LAB POS 2, δικαιούχο «Ιατρός», **3 ενεργά προϊόντα έκπτωσης**, 2 × ΝΕΡΟ 500ML / SKU `2269`, τιμή τεμαχίου **0,50 €**, έκπτωση **10%**, ποσό γραμμής **0,90 €** και κάτω σύνολο **0,90 €**. Έλαβα/ρέστα **0,00 €**, χωρίς ολοκλήρωση πληρωμής. Αυτό συμφωνεί με τη στρογγυλοποίηση της μειωμένης αξίας ανά γραμμή, 2 × 0,50 × 90% = 0,90 €.

Πριν από την εικόνα και μετά από ανανέωση του BackOffice: `LAB-POS-01` **1→1 κίνηση**, μετρητά/σύνολο **0,70→0,70 €**, κάρτες/IRIS **0,00 €**· `LAB-POS-02` **16→16 κινήσεις**, μετρητά **7,00→7,00 €**, κάρτες/IRIS **0,50→0,50 €**, σύνολο **7,50→7,50 €**. Δεν εμφανίστηκε νέα πώληση. Απόθεμα SKU 2269 και Audit αυτού του καλαθιού δεν επαναμετρήθηκαν και δεν δηλώνονται PASS. **PASS του συγκεκριμένου φυσικού καλαθιού για ποσό γραμμής και τελικό σύνολο στην ποσότητα 2.** Checkout/ρέστα με εισπραχθέν ποσό, άλλη ποσότητα, προσφορές, άλλος δικαιούχος/κατάστημα και ολικό Gate 4 παραμένουν NOT TESTED/OPEN. Δεν επαναλαμβάνεται αυτό το καλάθι για τεκμήριο.

**SOURCE FINDING / NOT LAB TESTED.** Issue #1202. Το σκέλος εκπτώσεων ανήκει στο ήδη ανατεθειμένο `agent/gate4-install-readiness`. Δεν έγινε νέα πώληση ή αλλαγή στο POS **σε αυτή την ανάγνωση κώδικα**. Ο ιδιοκτήτης διόρθωσε ότι η γενική δοκιμή έκπτωσης POS είχε ήδη γίνει· προγενέστερη καταγραφή 25/08 (`e14ba8f`) περιγράφει κάρτα δικαιούχου, server έλεγχο και στρογγυλοποίηση. Δεν χαρακτηρίζεται το προηγούμενο αποτέλεσμα αδοκίμαστο.

Στο `client/src/components/store/StorePosPanel.jsx` η `audiencePrice` στρογγυλοποιεί ανά τεμάχιο στο επόμενο 0,10 € και το `total` πολλαπλασιάζει με ποσότητα. Στο `server/src/routes/store-pos.js` η `applyAudienceDiscounts` στρογγυλοποιεί το σύνολο γραμμής. Για δύο τεμάχια 0,50 € και υποθετική ενεργή έκπτωση 10%, η οθόνη υπολογίζει 1,00 €, το checkout 0,90 €. Το παράδειγμα αποδεικνύει ασυμφωνία αλγορίθμων, όχι ότι το συγκεκριμένο LAB είδος έχει τέτοια έκπτωση ή ότι συνέβη χρέωση.

Πριν από φυσική δοκιμή χρειάζεται ενιαία αποτίμηση quote/checkout, regression πολλών τεμαχίων και ελέγχου προσφορών/barcode, πράσινο CI, merge και exact deploy. Αναζητείται το υπάρχον αριθμητικό LAB τεκμήριο, αν η προηγούμενη δοκιμή κάλυπτε ποσότητα >1. Καμία νέα φυσική πώληση μόνο για συμπλήρωση αρχείου. Δεν επαναλαμβάνεται η πώληση 23:22:35 ή η ήδη επιβεβαιωμένη επανεκτύπωση. Πραγματικό PASS/FAIL θα ενημερώσει κεντρικά manual, ενεργή λίστα, roadmap και PDF.

## Ανάληψη περιορισμένης διόρθωσης, 25/09

`agent/gate4-audience-line-total-20260925`: θα ευθυγραμμίσει το ποσό γραμμής, το κάτω σύνολο και τα ρέστα της οθόνης με τη στρογγυλοποίηση γραμμής του server για ποσότητα >1, χωρίς μεταβολή checkout/stock/βάρδιας/fiscal. Προστατεύονται το παλιό PASS της κάρτας δικαιούχου, οι υπάρχουσες πωλήσεις, το ποσό κανονικής τιμής χωρίς έκπτωση και η ήδη τελειωμένη επανεκτύπωση. Πριν το CI υπάρχει μόνο αριθμητικό εύρημα κώδικα, όχι νέο LAB FAIL/PASS. Μετά CI/merge/deploy, τυχόν φυσική αποδοχή θα βασιστεί σε υπάρχον τεκμήριο ή σε ανεξάρτητο νέο σκοπό, ποτέ σε επανάληψη μόνο για συμπλήρωση αρχείου.

## Περιορισμένη υλοποίηση προς CI, 25/09

Στο POS το εμφανιζόμενο ποσό γραμμής, το συνολικό ποσό, η μικτή πληρωμή που χρησιμοποιεί το σύνολο και τα ρέστα χρησιμοποιούν πλέον τον ίδιο κανόνα στρογγυλοποίησης ανά γραμμή με το υπάρχον checkout. Η κάρτα/barcode χρησιμοποιεί το κανονικό `productId` για αντιστοίχιση έκπτωσης. Χωρίς ενεργή έκπτωση δεν στρογγυλοποιείται αυθαίρετα η κανονική τιμή στο δεκάλεπτο. Αριθμητικός έλεγχος: 2 × 0,50 € / 10% → 0,90 €, 3 × 0,50 € / 10% → 1,40 €, 1 × 1,65 € / 0% → 1,65 €. Η γραμμή καλαθιού δείχνει το μειωμένο σύνολο και το ποσοστό.

Δεν αλλάζει ο server checkout, το ποσό που έχει ήδη καταχωριστεί, stock, βάρδια ή fiscal. Προσφορές, loyalty, συνδυασμοί διαφορετικών ειδών και πραγματικό LAB για αυτή την ειδική ποσότητα >1 δεν έχουν επιβεβαιωθεί από αυτή την αλλαγή. Η ήδη δοκιμασμένη έκπτωση δικαιούχου δεν επαναλαμβάνεται. Κατάσταση: **CODE READY / CI PENDING / LAB NOT TESTED** για το νέο ειδικό σενάριο.
