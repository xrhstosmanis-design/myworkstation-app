# Ομαδικός μηδενισμός καταμέτρησης

- Προστέθηκε επιλογή ανά γραμμή και επιλογή όλων των εμφανιζόμενων ειδών στην ανοικτή απογραφή.
- Η ενέργεια «Ομαδική καταμέτρηση: Μηδενισμός» γράφει καταμέτρηση `0` μόνο στα επιλεγμένα είδη του DRAFT.
- Δεν αλλάζει άμεσα το stock. Η αλλαγή αποθέματος παραμένει αποκλειστικά στην οριστικοποίηση.
- Κάθε γραμμή ελέγχει έκδοση, γράφει ξεχωριστό `InventoryCountEvent` και η παρτίδα είναι idempotent.
- **LAB PASS 11/09/2026:** επιλέχθηκαν και μηδενίστηκαν ομαδικά 10 μη καταμετρημένα είδη και επανακαταμετρήθηκαν ομαδικά οι 3 γραμμές με διαφορά.
- Η απογραφή οριστικοποιήθηκε `15/15`, με συνολική διαφορά `+10`, έλλειμμα `0` και πλεόνασμα `10`.
- Τα αρνητικά αποθέματα `-7`, `-2`, `-1` έγιναν `0` με ακριβώς μία κίνηση `STOCKTAKE_ADJUSTMENT` ανά είδος (`+7`, `+2`, `+1`).
- Ο τελικός έλεγχος στο `ΝΕΡΟ 1,5LT` έδειξε stock `0`, καταγεγραμμένες κινήσεις `0`, διαφορά `0` και πιθανές διπλές `0`.
