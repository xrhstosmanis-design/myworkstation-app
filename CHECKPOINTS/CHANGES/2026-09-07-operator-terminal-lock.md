# Operator terminal lock

PIN/card login is rejected when the same operator already has an open shift on another terminal. The response names the active terminal and requires Super Admin approval for a change.
