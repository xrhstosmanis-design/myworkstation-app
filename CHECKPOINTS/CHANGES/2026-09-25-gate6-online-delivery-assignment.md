# Gate 6 — Online παραγγελίες και Delivery — ανάθεση 25/09/2026

## Διόρθωση scope από τον ιδιοκτήτη

Το Gate 6 είναι η γενική λειτουργία **Online Ordering/Delivery του MyWorkStation**. Δεν είναι η εξωτερική διασύνδεση efood/Pelican. Η διασύνδεση και τα credentials παραμένουν ξεχωριστή εργασία και δεν αποτελούν προϋπόθεση κλεισίματος του Gate 6.

## Ιδιοκτησία

- Branch: `agent/gate6-online-delivery-20260925`
- Κατάσταση: **OPEN / ΣΕ ΕΛΕΓΧΟ — η πραγματική υποβολή αποκάλυψε σφάλμα checkout, χωρίς δημιουργία παραγγελίας**
- Κατάστημα πρώτου ελέγχου: `MYWORKSTATION LAB` / «ΕΡΓΑΣΤΗΡΙΟ ΔΟΚΙΜΩΝ».
- Δεν αγγίζει Gate 3, Gate 5, Gate 7 ή την εξωτερική σύνδεση efood/Pelican.

## Scope αποδοχής

1. Η παραγγελία ανήκει στο σωστό company/store και εμφανίζεται μόνο στο σωστό POS/BackOffice.
2. Υπάρχει ξεχωριστή, ευδιάκριτη ροή online/delivery και ετεροχρονισμένης χρέωσης.
3. Δημιουργία με idempotency key και retry δεν δημιουργούν δεύτερη παραγγελία.
4. Αποδοχή → προετοιμασία → έτοιμη → POS checkout → παράδοση συνδέουν μία μόνο πώληση.
5. Το απόθεμα μειώνεται ακριβώς μία φορά και retry ολοκλήρωσης δεν το ξαναμειώνει.
6. Ακύρωση/απόρριψη πριν από πώληση δεν δημιουργεί sale, fiscal ή stock κίνηση. Μετά από εμπορική καταχώριση εφαρμόζεται η ρητή ασφαλής πολιτική ακύρωσης/επιστροφής.
7. Τρόπος πληρωμής, βάρδια/terminal, χειριστής, ποσό και Audit συμφωνούν σε POS, BackOffice και αναφορές.
8. Η εξωτερική efood/Pelican σύνδεση δεν απαιτείται και δεν χρησιμοποιείται στην αποδοχή.

## Υπάρχον τεχνικό τεκμήριο πριν από αλλαγή

- Υπάρχει E2E `server/e2e/kat-online-ordering-flow.mjs` που καλύπτει δημιουργία, ίδιο idempotency retry, NEW→ACCEPTED→PREPARING→READY, POS checkout, DELIVERED, μία μείωση stock, πώληση `ONLINE_POS`, ταμείο και status events.
- Υπάρχουν μοναδικοί περιορισμοί `OnlineOrder(storeId,idempotencyKey)` και `OnlineOrder.saleId`, καθώς και guarded σύνδεση online order→sale μέσα στο POS checkout.
- Υπάρχει ήδη UI «DELIVERY / ΕΤΕΡΟΧΡΟΝΙΣΜΕΝΗ» και ξεχωριστό POS launcher παραγγελιών.
- Τα παραπάνω είναι **παλιό CI/E2E τεκμήριο**, όχι σημερινό πραγματικό LAB PASS.

## Πρώτο βήμα

Απογραφή του σημερινού production UI/API και εκτέλεση στοχευμένων tests χωρίς οικονομική LAB μεταβολή. Κάθε κενό διορθώνεται με στενή αλλαγή και regression test. Η φυσική δοκιμή θα γίνει συγκεντρωτικά με πλήρες πριν/μετά σε POS, stock και Audit, ώστε να μη δημιουργούνται επαναλαμβανόμενες δοκιμαστικές πωλήσεις.

## Production baseline πριν από την πρώτη ρύθμιση — 25/09/2026 20:00 ώρα Ελλάδας

- Το module «Ηλεκτρονικό κατάστημα / Παραγγελίες» είναι ενεργό στην άδεια `MYWORKSTATION LAB`.
- Το δημόσιο κατάστημα είναι ανενεργό, το δημόσιο slug είναι κενό και δεν υπάρχει ακόμη δημόσιο URL.
- Online προϊόντα: **0 από 67**.
- BackOffice → Online Παραγγελίες: σύνολο 0, ενεργές 0, παραδόθηκαν 0, με εμπορική πώληση 0, πιθανά duplicates 0.
- Ενεργές βάρδιες μάρτυρες: `MAIN` 0 συναλλαγές / 0,00 € και `LAB-POS-02` 1 συναλλαγή / 0,50 € μετρητά.
- Delivery και Pickup είναι διαθέσιμες ρυθμίσεις· μετρητά και κάρτα σε ασύρματο POS είναι ενεργές επιλογές. Ο έλεγχος διαθέσιμου stock είναι ανενεργός.
- Δεν έγινε ακόμη νέα online παραγγελία, πώληση ή stock κίνηση.

Η πρώτη ελεγχόμενη μεταβολή θα ενεργοποιήσει μόνο το LAB δημόσιο Online Store και ένα ταυτοποιημένο δοκιμαστικό προϊόν. Η ακριβής ποσότητα stock και τελευταία κίνηση θα αναγνωστούν πριν από την παραγγελία.

## Πρώτη ελεγχόμενη μεταβολή και εύρημα — 25/09/2026

- Ενεργοποιήθηκε το δημόσιο LAB Online Store με slug `myworkstation-lab`.
- Δημοσιεύτηκε ακριβώς ένα προϊόν: **ΝΕΡΟ 500ML — SKU 2269 — 0,50 €**. Το προσωρινά επιλεγμένο `LAB EXCEL TEST 1` αφαιρέθηκε από το Online Store.
- Δεν δημιουργήθηκε online παραγγελία, εμπορική πώληση ή νέα κίνηση stock.
- Η δημόσια σελίδα επιβεβαίωσε σωστά κατάστημα, τιμή, μηδενική προσαύξηση, Delivery/Pickup και μετρητά/κάρτα σε ασύρματο POS.
- Εντοπίστηκε ασυνέπεια: η ροή `catalog-modifiers` εμφάνιζε το SKU 2269 ως «Εξαντλήθηκε» λόγω αρνητικού stock, παρότι η ρύθμιση «Έλεγχος διαθέσιμου stock» ήταν ανενεργή.
- Διορθώθηκε η διαθεσιμότητα και η υποβολή παραγγελίας ώστε το stock να επιβάλλεται μόνο όταν `stockCheckEnabled=true`. Η ίδια πολιτική εφαρμόζεται πλέον στην εμφάνιση και στο server-side submit.
- Προστέθηκε regression test για αρνητικό stock με τον έλεγχο ανενεργό, καθώς και για επαρκή/ανεπαρκή ποσότητα όταν ο έλεγχος είναι ενεργός.

## Τεχνικό αποτέλεσμα

- Συνολικό Gate 6 πακέτο: **68 PASS / 0 FAIL**.
- Τελευταίο στοχευμένο πακέτο `online-store-*.test.js`: **21 PASS / 0 FAIL**.
- Επόμενο βήμα μετά το deployment της διόρθωσης: επαλήθευση ότι το SKU 2269 είναι διαθέσιμο, καταγραφή ακριβούς stock/τελευταίας κίνησης και μία συγκεντρωτική φυσική παραγγελία μέχρι POS checkout, παράδοση και τελικό Audit.

## Εύρημα πραγματικού checkout — 25/09/2026

- Μετά από ρητή έγκριση επιχειρήθηκε μία υποβολή Delivery για **1 × ΝΕΡΟ 500ML — SKU 2269 — 0,50 €**, με πληρωμή «Κάρτα στο ασύρματο POS».
- Η υποβολή σταμάτησε τοπικά πριν από το API με `TypeError` και **δεν δημιουργήθηκε παραγγελία, πώληση ή κίνηση stock**.
- Αιτία: το checkout βασιζόταν στην αυτόματη named-property μεταβλητή `name`, η οποία συγκρούεται με το ενσωματωμένο `window.name` του browser.
- Διόρθωση: όλα τα πεδία παραγγελίας διαβάζονται πλέον από ρητές αναφορές `document.getElementById` μέσω `orderFields`.
- Προστέθηκε regression test που απαγορεύει την παλιά ανάγνωση `name.value` και επιβεβαιώνει τη νέα ρητή ανάγνωση.
- Στοχευμένο πακέτο `online-store-*.test.js`: **22 PASS / 0 FAIL**.
- Το Gate 6 παραμένει **OPEN** μέχρι deployment και μία επιτυχημένη συγκεντρωτική φυσική ροή δημιουργία → σωστό POS/BackOffice → checkout → παράδοση → Audit.
- Μετά το deployment του checkout fix, η εγκεκριμένη υποβολή έφτασε στο API αλλά επέστρεψε γενικό HTTP 500. Η ίδια idempotency key επαναχρησιμοποιήθηκε μόνο για επιβεβαίωση και δεν υπήρξε επιτυχής δημιουργία. Προστέθηκε ασφαλής κωδικοποίηση σταδίου (`LOCK`, `SERIAL`, `ORDER`, `LINES`, `EVENT`) ώστε το production να εντοπίσει το σημείο αστοχίας χωρίς έκθεση στοιχείων βάσης.
- Το production diagnostic επέστρεψε `LOCK`: το `pg_advisory_xact_lock`, που επιστρέφει PostgreSQL `void`, εκτελούνταν λανθασμένα με `$queryRaw`. Διορθώθηκε σε `$executeRaw`. Η ίδια idempotency key θα χρησιμοποιηθεί μετά το deploy.
