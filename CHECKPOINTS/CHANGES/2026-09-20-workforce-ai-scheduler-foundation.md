# 2026-09-20 — Workforce AI Scheduler foundation — AWAITING LAB

Adds the conversational scheduler workspace and a fail-closed preview endpoint. User writes natural Greek instructions; backend returns only verified Workforce context: active employees, permanent rules, availability, approved leave and shift templates. No assignment is written and no schedule is published. Next step is structured AI proposal → deterministic validation → conversational corrections → explicit approval/publish.
