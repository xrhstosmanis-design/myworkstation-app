# 2026-09-20 — Natural-language employee rules v1 — AWAITING LAB

Employee edit card now supports natural-language permanent rules with effective date, interpretation preview, explicit confirmation and history. v1 safely compiles only recognized constraints (only morning/afternoon/night, N workdays/week, continuous-days-off preference metadata) into structured legacy scheduler fields. Existing free text remains in immutable history. New employee must first be saved before adding rules.
