# Invoice Learning — πολυσέλιδο upload

## Κατάσταση

**POST-DEPLOY LAB FAIL / HOTFIX LOCAL PASS / AWAITING CI + DEPLOY + LAB.**

Στο πραγματικό Gate 3 δείγμα ΤΑΛΩΣ (`01T00125909`, δύο φωτογραφημένες
σελίδες, σύνολο `252,06 €`) το Invoice Learning εμφανίζει επιλογή μόνο ενός
αρχείου. Έτσι δεν μπορεί να εκπαιδευτεί ολόκληρο το παραστατικό πριν από νέα
δοκιμή POS.

## Bounded αλλαγή

- Η επιλογή «Φωτογραφία» θα δέχεται έως 5 σελίδες του ίδιου τιμολογίου.
- Οι επιλεγμένες σελίδες θα εμφανίζονται με τη σειρά τους και θα αποστέλλονται
  μαζί στην υπάρχουσα Azure → AI ανάγνωση.
- Ένα ήδη πολυσέλιδο PDF παραμένει ένα αρχείο. Δεν επιτρέπεται ανάμιξη PDF με
  άλλες φωτογραφίες στην ίδια εκμάθηση.
- Η Azure ανάγνωση θα ενώνει τις φυσικές γραμμές όλων των σελίδων πριν από τον
  υπάρχοντα οικονομικό έλεγχο. Το OpenAI fallback θα λαμβάνει επίσης όλες τις
  σελίδες στην ίδια κλήση.

## Προστατευόμενες συμπεριφορές

- Καμία δημιουργία/μεταβολή πληρωμής, purchase draft, stock, οριστικοποίησης,
  λογιστικής, fiscal ή myDATA.
- Η εκμάθηση παραμένει εποπτευόμενη και fail-closed ως προς την οικονομική
  συμφωνία.
- Η σειρά των σελίδων και όλες οι επαναλαμβανόμενες φυσικές γραμμές
  διατηρούνται.
- Η μονή φωτογραφία, η κάμερα, το QR και το PDF συνεχίζουν να λειτουργούν.

## Αποδοχή

1. Πράσινα targeted tests, πλήρες server suite και client build.
2. Πράσινο CI, merge και επιβεβαίωση ακριβούς Render revision.
3. **LAB:** επιλογή και των δύο φωτογραφιών ΤΑΛΩΣ με μία ενέργεια.
4. Το Learning πρέπει να εμφανίσει 44 πραγματικές γραμμές και να συμφωνήσει
   `223,05 € + 29,01 € = 252,06 €` πριν από «Επιβεβαίωση & Εκμάθηση».

Μέχρι το βήμα 4 η αλλαγή παραμένει **AWAITING LAB** και το λανθασμένο draft
`700,61 € / 757,63 €` δεν οριστικοποιείται.

## Τοπική επαλήθευση

- Targeted multipage/mobile/completeness/image-quality tests: `23/23` PASS.
- Πλήρες server suite: `1402/1402` PASS.
- Client production build: PASS.
- Server build / Prisma generation: PASS.
- Δεν εκτελέστηκε database migration, πληρωμή, stock posting ή οριστικοποίηση.

## Post-deploy λευκή σελίδα και hotfix

Μετά το πρώτο deploy του πολυσέλιδου upload, το πραγματικό LAB άνοιξε λευκή
σελίδα. Αιτία: ο installer έγραφε το ίδιο `textContent` του κουμπιού σε κάθε
εκτέλεση του global `MutationObserver`. Η εγγραφή δημιουργούσε νέα mutation και
αμέσως νέα εκτέλεση, με αποτέλεσμα ατέρμονα κύκλο στο main thread.

Το hotfix κάνει την αλλαγή idempotent: γράφει την ετικέτα μόνο όταν είναι
διαφορετική. Δεν αλλάζει upload, AI, οικονομικά δεδομένα ή άλλο route.
