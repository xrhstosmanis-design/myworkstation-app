NEXT: deploy and live-verify the same-origin «Άνοιγμα σε αυτό το PC» terminal activation action. Use it only for the already mapped `KAT-POS-02`, sign in with the existing personal operator flow, create one controlled completed NON_FISCAL card sale and execute Fiscal DRY RUN twice to verify idempotency. Do not retry from `MAIN`, do not activate commercially locked modules and do not send any fiscal/RBS/CapDriver/EFTPOS command. Then continue with `MOD-02`.
