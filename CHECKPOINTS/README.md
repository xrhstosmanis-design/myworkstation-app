# MyWorkStation checkpoints

Always read `CHECKPOINTS/ACTIVE.md` first when continuing implementation in a new session.

Update the active checkpoint after every meaningful implementation block with:
- branch and commit/PR
- CI status
- Render/live status
- completed functionality
- regression/live tests performed
- exact next step
- items that can be removed from the user's pending list only after required tests pass
