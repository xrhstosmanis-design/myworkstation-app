Active checkpoint: `CHECKPOINTS/CHANGES/2026-09-12-invoice-report-rounded-totals.md` (with unified list `CHECKPOINTS/KAT_ACTIVE_LIST_2026-09-05.md`)

Current branch: `feature/invoice-report-rounded-totals`

Current goal: make the purchase-order report and summary use the same two-decimal line totals already verified in invoice detail.

Rule: every code/configuration PR must update the active unified list and add a checkpoint under `CHECKPOINTS/CHANGES/`. CI enforces the rule. Completed, evidenced items are marked `ΟΚ`; untested work remains `ΣΕ ΔΟΚΙΜΗ`.
