Active checkpoint: `CHECKPOINTS/CHANGES/2026-09-06-home-04-terminal-mapping-blocker.md` (with unified list `CHECKPOINTS/KAT_ACTIVE_LIST_2026-09-05.md`)

Current branch: `agent/home-04-terminal-mapping-blocker-20260906`

Current goal: complete the controlled NON_FISCAL Fiscal DRY RUN prerequisite through a terminal with an explicit stored mapping. Do not retry from `MAIN`. Use a dedicated `KAT-HOME-TEST` mapping/activation or the already mapped `KAT-POS-02`; then run the same safe DRY RUN twice and continue with `MOD-02`.

Rule: every code/configuration PR must update the active unified list and add a checkpoint under `CHECKPOINTS/CHANGES/`. CI enforces the rule. Completed, evidenced items are marked `ΟΚ`; untested work remains `ΣΕ ΔΟΚΙΜΗ`.
