Active checkpoint: `CHECKPOINTS/KAT_ACTIVE_LIST_2026-09-05.md`

Current branch: `feat/super-admin-fiscal-dry-run-screen` / PR #538

Current goal: complete `HOME-04` with a protected Platform Super Admin screen, green CI/merge and one live HOME PC DRY RUN. The screen must remain fail-closed unless RBS write, CapDriver write, fiscal issuance and external execution are all explicitly false.

Rule: every code/configuration PR must update the active unified list and add a checkpoint under `CHECKPOINTS/CHANGES/`. CI enforces the rule. Completed, evidenced items are marked `ΟΚ`; untested work remains `ΣΕ ΔΟΚΙΜΗ`.
