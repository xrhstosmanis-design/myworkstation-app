Active checkpoint: `CHECKPOINTS/CHANGES/2026-09-12-platform-audit-resizable-table.md` (with unified list `CHECKPOINTS/KAT_ACTIVE_LIST_2026-09-05.md`)

Current branch: `feature/platform-audit-resizable-table`

Current goal: make the Super Admin Events table full-size and allow operator-controlled column widths without editing immutable Audit data.

Rule: every code/configuration PR must update the active unified list and add a checkpoint under `CHECKPOINTS/CHANGES/`. CI enforces the rule. Completed, evidenced items are marked `ΟΚ`; untested work remains `ΣΕ ΔΟΚΙΜΗ`.
