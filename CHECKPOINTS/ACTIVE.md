Active checkpoint: `CHECKPOINTS/CHANGES/2026-09-06-home-04-safe-terminal-self-activation.md` (with unified list `CHECKPOINTS/KAT_ACTIVE_LIST_2026-09-05.md`)

Current branch: `agent/home-04-open-activation-link`

Current goal: deploy the safe same-origin «Άνοιγμα σε αυτό το PC» activation action, bind the controlled browser to the already mapped `KAT-POS-02`, create one controlled NON_FISCAL card sale, run the same safe DRY RUN twice and continue with `MOD-02`. Do not retry from `MAIN`.

Rule: every code/configuration PR must update the active unified list and add a checkpoint under `CHECKPOINTS/CHANGES/`. CI enforces the rule. Completed, evidenced items are marked `ΟΚ`; untested work remains `ΣΕ ΔΟΚΙΜΗ`.
