Active checkpoint: `CHECKPOINTS/CHANGES/2026-09-12-global-resizable-tables.md` (with unified list `CHECKPOINTS/KAT_ACTIVE_LIST_2026-09-05.md`)

Current branch: `feature/global-resizable-tables`

Current goal: replace broken native table resize bars with the shared visible column handle across BackOffice and Platform tables.

Rule: every code/configuration PR must update the active unified list and add a checkpoint under `CHECKPOINTS/CHANGES/`. CI enforces the rule. Completed, evidenced items are marked `ΟΚ`; untested work remains `ΣΕ ΔΟΚΙΜΗ`.
