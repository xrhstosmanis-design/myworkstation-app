import {prisma} from "./prisma.js";

const statements=[
`CREATE TABLE IF NOT EXISTS "ProductCategory" (
  "id" TEXT NOT NULL,
  "companyId" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "sortOrder" INTEGER NOT NULL DEFAULT 0,
  "active" BOOLEAN NOT NULL DEFAULT true,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updatedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "ProductCategory_pkey" PRIMARY KEY ("id"),
  CONSTRAINT "ProductCategory_companyId_fkey" FOREIGN KEY ("companyId") REFERENCES "Company"("id") ON DELETE CASCADE ON UPDATE CASCADE
)`,
`CREATE UNIQUE INDEX IF NOT EXISTS "ProductCategory_companyId_name_key" ON "ProductCategory"("companyId","name")`,
`CREATE INDEX IF NOT EXISTS "ProductCategory_companyId_idx" ON "ProductCategory"("companyId")`,

`CREATE TABLE IF NOT EXISTS "Product" (
  "id" TEXT NOT NULL,
  "companyId" TEXT NOT NULL,
  "categoryId" TEXT,
  "sku" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "unit" TEXT NOT NULL DEFAULT 'PIECE',
  "vatRate" DECIMAL(6,3) NOT NULL DEFAULT 24,
  "salePrice" DECIMAL(14,4) NOT NULL DEFAULT 0,
  "costPrice" DECIMAL(14,4) NOT NULL DEFAULT 0,
  "trackStock" BOOLEAN NOT NULL DEFAULT true,
  "active" BOOLEAN NOT NULL DEFAULT true,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updatedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "Product_pkey" PRIMARY KEY ("id"),
  CONSTRAINT "Product_companyId_fkey" FOREIGN KEY ("companyId") REFERENCES "Company"("id") ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT "Product_categoryId_fkey" FOREIGN KEY ("categoryId") REFERENCES "ProductCategory"("id") ON DELETE SET NULL ON UPDATE CASCADE
)`,
`CREATE UNIQUE INDEX IF NOT EXISTS "Product_companyId_sku_key" ON "Product"("companyId","sku") WHERE "sku" IS NOT NULL`,
`CREATE INDEX IF NOT EXISTS "Product_companyId_idx" ON "Product"("companyId")`,
`CREATE INDEX IF NOT EXISTS "Product_categoryId_idx" ON "Product"("categoryId")`,
`CREATE INDEX IF NOT EXISTS "Product_name_idx" ON "Product"("name")`,

`CREATE TABLE IF NOT EXISTS "ProductBarcode" (
  "id" TEXT NOT NULL,
  "productId" TEXT NOT NULL,
  "barcode" TEXT NOT NULL,
  "unitMultiplier" DECIMAL(14,4) NOT NULL DEFAULT 1,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "ProductBarcode_pkey" PRIMARY KEY ("id"),
  CONSTRAINT "ProductBarcode_productId_fkey" FOREIGN KEY ("productId") REFERENCES "Product"("id") ON DELETE CASCADE ON UPDATE CASCADE
)`,
`DROP INDEX IF EXISTS "ProductBarcode_barcode_key"`,
`CREATE UNIQUE INDEX IF NOT EXISTS "ProductBarcode_productId_barcode_key" ON "ProductBarcode"("productId","barcode")`,
`CREATE INDEX IF NOT EXISTS "ProductBarcode_barcode_idx" ON "ProductBarcode"("barcode")`,
`CREATE INDEX IF NOT EXISTS "ProductBarcode_productId_idx" ON "ProductBarcode"("productId")`,

`CREATE TABLE IF NOT EXISTS "StoreProduct" (
  "id" TEXT NOT NULL,
  "storeId" TEXT NOT NULL,
  "productId" TEXT NOT NULL,
  "salePrice" DECIMAL(14,4),
  "minStock" DECIMAL(14,4),
  "currentStock" DECIMAL(14,4) NOT NULL DEFAULT 0,
  "active" BOOLEAN NOT NULL DEFAULT true,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updatedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "StoreProduct_pkey" PRIMARY KEY ("id"),
  CONSTRAINT "StoreProduct_storeId_fkey" FOREIGN KEY ("storeId") REFERENCES "Store"("id") ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT "StoreProduct_productId_fkey" FOREIGN KEY ("productId") REFERENCES "Product"("id") ON DELETE CASCADE ON UPDATE CASCADE
)`,
`CREATE UNIQUE INDEX IF NOT EXISTS "StoreProduct_storeId_productId_key" ON "StoreProduct"("storeId","productId")`,
`CREATE INDEX IF NOT EXISTS "StoreProduct_storeId_idx" ON "StoreProduct"("storeId")`,
`CREATE INDEX IF NOT EXISTS "StoreProduct_productId_idx" ON "StoreProduct"("productId")`,

`CREATE TABLE IF NOT EXISTS "Supplier" (
  "id" TEXT NOT NULL,
  "companyId" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "taxId" TEXT,
  "email" TEXT,
  "phone" TEXT,
  "address" TEXT,
  "city" TEXT,
  "active" BOOLEAN NOT NULL DEFAULT true,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updatedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "Supplier_pkey" PRIMARY KEY ("id"),
  CONSTRAINT "Supplier_companyId_fkey" FOREIGN KEY ("companyId") REFERENCES "Company"("id") ON DELETE CASCADE ON UPDATE CASCADE
)`,
`CREATE INDEX IF NOT EXISTS "Supplier_companyId_idx" ON "Supplier"("companyId")`,
`CREATE UNIQUE INDEX IF NOT EXISTS "Supplier_companyId_taxId_key" ON "Supplier"("companyId","taxId") WHERE "taxId" IS NOT NULL`,

`CREATE TABLE IF NOT EXISTS "PurchaseDocument" (
  "id" TEXT NOT NULL,
  "companyId" TEXT NOT NULL,
  "storeId" TEXT NOT NULL,
  "supplierId" TEXT,
  "documentType" TEXT NOT NULL DEFAULT 'INVOICE',
  "documentNumber" TEXT,
  "documentDate" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "totalNet" DECIMAL(14,4) NOT NULL DEFAULT 0,
  "totalVat" DECIMAL(14,4) NOT NULL DEFAULT 0,
  "totalGross" DECIMAL(14,4) NOT NULL DEFAULT 0,
  "sourceType" TEXT NOT NULL DEFAULT 'MANUAL',
  "status" TEXT NOT NULL DEFAULT 'DRAFT',
  "imageRef" TEXT,
  "aiConfidence" DECIMAL(6,3),
  "createdByUserId" TEXT,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updatedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "PurchaseDocument_pkey" PRIMARY KEY ("id"),
  CONSTRAINT "PurchaseDocument_companyId_fkey" FOREIGN KEY ("companyId") REFERENCES "Company"("id") ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT "PurchaseDocument_storeId_fkey" FOREIGN KEY ("storeId") REFERENCES "Store"("id") ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT "PurchaseDocument_supplierId_fkey" FOREIGN KEY ("supplierId") REFERENCES "Supplier"("id") ON DELETE SET NULL ON UPDATE CASCADE,
  CONSTRAINT "PurchaseDocument_createdByUserId_fkey" FOREIGN KEY ("createdByUserId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
)`,
`CREATE INDEX IF NOT EXISTS "PurchaseDocument_companyId_idx" ON "PurchaseDocument"("companyId")`,
`CREATE INDEX IF NOT EXISTS "PurchaseDocument_storeId_idx" ON "PurchaseDocument"("storeId")`,
`CREATE INDEX IF NOT EXISTS "PurchaseDocument_supplierId_idx" ON "PurchaseDocument"("supplierId")`,
`CREATE INDEX IF NOT EXISTS "PurchaseDocument_documentDate_idx" ON "PurchaseDocument"("documentDate")`,

`CREATE TABLE IF NOT EXISTS "PurchaseDocumentLine" (
  "id" TEXT NOT NULL,
  "purchaseDocumentId" TEXT NOT NULL,
  "productId" TEXT,
  "description" TEXT NOT NULL,
  "quantity" DECIMAL(14,4) NOT NULL DEFAULT 1,
  "unit" TEXT NOT NULL DEFAULT 'PIECE',
  "unitsPerPackage" DECIMAL(14,4),
  "unitCost" DECIMAL(14,4) NOT NULL DEFAULT 0,
  "netAmount" DECIMAL(14,4) NOT NULL DEFAULT 0,
  "vatRate" DECIMAL(6,3) NOT NULL DEFAULT 24,
  "vatAmount" DECIMAL(14,4) NOT NULL DEFAULT 0,
  "grossAmount" DECIMAL(14,4) NOT NULL DEFAULT 0,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "PurchaseDocumentLine_pkey" PRIMARY KEY ("id"),
  CONSTRAINT "PurchaseDocumentLine_documentId_fkey" FOREIGN KEY ("purchaseDocumentId") REFERENCES "PurchaseDocument"("id") ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT "PurchaseDocumentLine_productId_fkey" FOREIGN KEY ("productId") REFERENCES "Product"("id") ON DELETE SET NULL ON UPDATE CASCADE
)`,
`CREATE INDEX IF NOT EXISTS "PurchaseDocumentLine_documentId_idx" ON "PurchaseDocumentLine"("purchaseDocumentId")`,
`CREATE INDEX IF NOT EXISTS "PurchaseDocumentLine_productId_idx" ON "PurchaseDocumentLine"("productId")`,

`CREATE TABLE IF NOT EXISTS "StockMovement" (
  "id" TEXT NOT NULL,
  "storeId" TEXT NOT NULL,
  "productId" TEXT NOT NULL,
  "movementType" TEXT NOT NULL,
  "quantity" DECIMAL(14,4) NOT NULL,
  "unitCost" DECIMAL(14,4),
  "sourceType" TEXT,
  "sourceId" TEXT,
  "note" TEXT,
  "createdByUserId" TEXT,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "StockMovement_pkey" PRIMARY KEY ("id"),
  CONSTRAINT "StockMovement_storeId_fkey" FOREIGN KEY ("storeId") REFERENCES "Store"("id") ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT "StockMovement_productId_fkey" FOREIGN KEY ("productId") REFERENCES "Product"("id") ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT "StockMovement_createdByUserId_fkey" FOREIGN KEY ("createdByUserId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
)`,
`CREATE INDEX IF NOT EXISTS "StockMovement_store_product_idx" ON "StockMovement"("storeId","productId")`,
`CREATE INDEX IF NOT EXISTS "StockMovement_createdAt_idx" ON "StockMovement"("createdAt")`,
`ALTER TABLE "StockMovement" ADD COLUMN IF NOT EXISTS "idempotencyKey" TEXT`,
`CREATE UNIQUE INDEX IF NOT EXISTS "StockMovement_store_idempotency_key" ON "StockMovement"("storeId","idempotencyKey") WHERE "idempotencyKey" IS NOT NULL`,

`CREATE TABLE IF NOT EXISTS "Recipe" (
  "id" TEXT NOT NULL,
  "companyId" TEXT NOT NULL,
  "productId" TEXT NOT NULL,
  "yieldQuantity" DECIMAL(14,4) NOT NULL DEFAULT 1,
  "active" BOOLEAN NOT NULL DEFAULT true,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updatedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "Recipe_pkey" PRIMARY KEY ("id"),
  CONSTRAINT "Recipe_companyId_fkey" FOREIGN KEY ("companyId") REFERENCES "Company"("id") ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT "Recipe_productId_fkey" FOREIGN KEY ("productId") REFERENCES "Product"("id") ON DELETE CASCADE ON UPDATE CASCADE
)`,
`CREATE UNIQUE INDEX IF NOT EXISTS "Recipe_productId_key" ON "Recipe"("productId")`,
`CREATE INDEX IF NOT EXISTS "Recipe_companyId_idx" ON "Recipe"("companyId")`,

`CREATE TABLE IF NOT EXISTS "RecipeItem" (
  "id" TEXT NOT NULL,
  "recipeId" TEXT NOT NULL,
  "ingredientProductId" TEXT NOT NULL,
  "quantity" DECIMAL(14,4) NOT NULL,
  "unit" TEXT NOT NULL DEFAULT 'PIECE',
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "RecipeItem_pkey" PRIMARY KEY ("id"),
  CONSTRAINT "RecipeItem_recipeId_fkey" FOREIGN KEY ("recipeId") REFERENCES "Recipe"("id") ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT "RecipeItem_ingredientProductId_fkey" FOREIGN KEY ("ingredientProductId") REFERENCES "Product"("id") ON DELETE CASCADE ON UPDATE CASCADE
)`,
`CREATE UNIQUE INDEX IF NOT EXISTS "RecipeItem_recipe_ingredient_key" ON "RecipeItem"("recipeId","ingredientProductId")`,

`CREATE TABLE IF NOT EXISTS "Customer" (
  "id" TEXT NOT NULL,
  "companyId" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "taxId" TEXT,
  "phone" TEXT,
  "email" TEXT,
  "discountPercent" DECIMAL(6,3) NOT NULL DEFAULT 0,
  "creditLimit" DECIMAL(14,4) NOT NULL DEFAULT 0,
  "balance" DECIMAL(14,4) NOT NULL DEFAULT 0,
  "active" BOOLEAN NOT NULL DEFAULT true,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updatedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "Customer_pkey" PRIMARY KEY ("id"),
  CONSTRAINT "Customer_companyId_fkey" FOREIGN KEY ("companyId") REFERENCES "Company"("id") ON DELETE CASCADE ON UPDATE CASCADE
)`,
`CREATE INDEX IF NOT EXISTS "Customer_companyId_idx" ON "Customer"("companyId")`,

`CREATE TABLE IF NOT EXISTS "Sale" (
  "id" TEXT NOT NULL,
  "companyId" TEXT NOT NULL,
  "storeId" TEXT NOT NULL,
  "operatorEmployeeId" TEXT,
  "customerId" TEXT,
  "receiptNumber" TEXT,
  "fiscalStatus" TEXT NOT NULL DEFAULT 'NON_FISCAL',
  "subtotal" DECIMAL(14,4) NOT NULL DEFAULT 0,
  "discount" DECIMAL(14,4) NOT NULL DEFAULT 0,
  "total" DECIMAL(14,4) NOT NULL DEFAULT 0,
  "status" TEXT NOT NULL DEFAULT 'COMPLETED',
  "source" TEXT NOT NULL DEFAULT 'POS',
  "occurredAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "Sale_pkey" PRIMARY KEY ("id"),
  CONSTRAINT "Sale_companyId_fkey" FOREIGN KEY ("companyId") REFERENCES "Company"("id") ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT "Sale_storeId_fkey" FOREIGN KEY ("storeId") REFERENCES "Store"("id") ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT "Sale_operatorEmployeeId_fkey" FOREIGN KEY ("operatorEmployeeId") REFERENCES "Employee"("id") ON DELETE SET NULL ON UPDATE CASCADE,
  CONSTRAINT "Sale_customerId_fkey" FOREIGN KEY ("customerId") REFERENCES "Customer"("id") ON DELETE SET NULL ON UPDATE CASCADE
)`,
`CREATE INDEX IF NOT EXISTS "Sale_companyId_idx" ON "Sale"("companyId")`,
`CREATE INDEX IF NOT EXISTS "Sale_store_occurredAt_idx" ON "Sale"("storeId","occurredAt")`,
`CREATE INDEX IF NOT EXISTS "Sale_operatorEmployeeId_idx" ON "Sale"("operatorEmployeeId")`,

`CREATE TABLE IF NOT EXISTS "SaleLine" (
  "id" TEXT NOT NULL,
  "saleId" TEXT NOT NULL,
  "productId" TEXT,
  "description" TEXT NOT NULL,
  "quantity" DECIMAL(14,4) NOT NULL DEFAULT 1,
  "unitPrice" DECIMAL(14,4) NOT NULL DEFAULT 0,
  "discount" DECIMAL(14,4) NOT NULL DEFAULT 0,
  "vatRate" DECIMAL(6,3) NOT NULL DEFAULT 24,
  "lineTotal" DECIMAL(14,4) NOT NULL DEFAULT 0,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "SaleLine_pkey" PRIMARY KEY ("id"),
  CONSTRAINT "SaleLine_saleId_fkey" FOREIGN KEY ("saleId") REFERENCES "Sale"("id") ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT "SaleLine_productId_fkey" FOREIGN KEY ("productId") REFERENCES "Product"("id") ON DELETE SET NULL ON UPDATE CASCADE
)`,
`CREATE INDEX IF NOT EXISTS "SaleLine_saleId_idx" ON "SaleLine"("saleId")`,
`CREATE INDEX IF NOT EXISTS "SaleLine_productId_idx" ON "SaleLine"("productId")`,

`CREATE TABLE IF NOT EXISTS "Payment" (
  "id" TEXT NOT NULL,
  "saleId" TEXT NOT NULL,
  "method" TEXT NOT NULL,
  "amount" DECIMAL(14,4) NOT NULL,
  "terminalRef" TEXT,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "Payment_pkey" PRIMARY KEY ("id"),
  CONSTRAINT "Payment_saleId_fkey" FOREIGN KEY ("saleId") REFERENCES "Sale"("id") ON DELETE CASCADE ON UPDATE CASCADE
)`,
`CREATE INDEX IF NOT EXISTS "Payment_saleId_idx" ON "Payment"("saleId")`,

`CREATE TABLE IF NOT EXISTS "CustomerLedger" (
  "id" TEXT NOT NULL,
  "customerId" TEXT NOT NULL,
  "storeId" TEXT,
  "entryType" TEXT NOT NULL,
  "amount" DECIMAL(14,4) NOT NULL,
  "referenceType" TEXT,
  "referenceId" TEXT,
  "note" TEXT,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "CustomerLedger_pkey" PRIMARY KEY ("id"),
  CONSTRAINT "CustomerLedger_customerId_fkey" FOREIGN KEY ("customerId") REFERENCES "Customer"("id") ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT "CustomerLedger_storeId_fkey" FOREIGN KEY ("storeId") REFERENCES "Store"("id") ON DELETE SET NULL ON UPDATE CASCADE
)`,
`CREATE INDEX IF NOT EXISTS "CustomerLedger_customerId_idx" ON "CustomerLedger"("customerId")`,

`CREATE TABLE IF NOT EXISTS "Expense" (
  "id" TEXT NOT NULL,
  "companyId" TEXT NOT NULL,
  "storeId" TEXT,
  "supplierId" TEXT,
  "expenseType" TEXT NOT NULL,
  "description" TEXT NOT NULL,
  "amount" DECIMAL(14,4) NOT NULL,
  "paymentMethod" TEXT,
  "occurredAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "createdByUserId" TEXT,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "Expense_pkey" PRIMARY KEY ("id"),
  CONSTRAINT "Expense_companyId_fkey" FOREIGN KEY ("companyId") REFERENCES "Company"("id") ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT "Expense_storeId_fkey" FOREIGN KEY ("storeId") REFERENCES "Store"("id") ON DELETE SET NULL ON UPDATE CASCADE,
  CONSTRAINT "Expense_supplierId_fkey" FOREIGN KEY ("supplierId") REFERENCES "Supplier"("id") ON DELETE SET NULL ON UPDATE CASCADE,
  CONSTRAINT "Expense_createdByUserId_fkey" FOREIGN KEY ("createdByUserId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
)`,
`CREATE INDEX IF NOT EXISTS "Expense_company_occurredAt_idx" ON "Expense"("companyId","occurredAt")`,

`CREATE TABLE IF NOT EXISTS "ShiftHandover" (
  "id" TEXT NOT NULL,
  "storeId" TEXT NOT NULL,
  "fromEmployeeId" TEXT,
  "toEmployeeId" TEXT,
  "priority" TEXT NOT NULL DEFAULT 'NORMAL',
  "message" TEXT NOT NULL,
  "status" TEXT NOT NULL DEFAULT 'OPEN',
  "fromName" TEXT,
  "acknowledgedById" TEXT,
  "acknowledgedByName" TEXT,
  "attachmentData" TEXT,
  "attachmentMimeType" TEXT,
  "attachmentFilename" TEXT,
  "attachmentChecksum" TEXT,
  "acknowledgedAt" TIMESTAMP(3),
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "ShiftHandover_pkey" PRIMARY KEY ("id"),
  CONSTRAINT "ShiftHandover_storeId_fkey" FOREIGN KEY ("storeId") REFERENCES "Store"("id") ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT "ShiftHandover_fromEmployeeId_fkey" FOREIGN KEY ("fromEmployeeId") REFERENCES "Employee"("id") ON DELETE SET NULL ON UPDATE CASCADE,
  CONSTRAINT "ShiftHandover_toEmployeeId_fkey" FOREIGN KEY ("toEmployeeId") REFERENCES "Employee"("id") ON DELETE SET NULL ON UPDATE CASCADE
)`,
`CREATE INDEX IF NOT EXISTS "ShiftHandover_store_status_idx" ON "ShiftHandover"("storeId","status")`,
`ALTER TABLE "ShiftHandover" ADD COLUMN IF NOT EXISTS "fromName" TEXT`,
`ALTER TABLE "ShiftHandover" ADD COLUMN IF NOT EXISTS "acknowledgedById" TEXT`,
`ALTER TABLE "ShiftHandover" ADD COLUMN IF NOT EXISTS "acknowledgedByName" TEXT`,
`ALTER TABLE "ShiftHandover" ADD COLUMN IF NOT EXISTS "attachmentData" TEXT`,
`ALTER TABLE "ShiftHandover" ADD COLUMN IF NOT EXISTS "attachmentMimeType" TEXT`,
`ALTER TABLE "ShiftHandover" ADD COLUMN IF NOT EXISTS "attachmentFilename" TEXT`,
`ALTER TABLE "ShiftHandover" ADD COLUMN IF NOT EXISTS "attachmentChecksum" TEXT`,

`CREATE TABLE IF NOT EXISTS "DocumentAttachment" (
  "id" TEXT NOT NULL,
  "companyId" TEXT NOT NULL,
  "storeId" TEXT,
  "documentType" TEXT NOT NULL,
  "filename" TEXT,
  "mimeType" TEXT,
  "storageKey" TEXT NOT NULL,
  "contentData" TEXT,
  "checksum" TEXT,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "DocumentAttachment_pkey" PRIMARY KEY ("id"),
  CONSTRAINT "DocumentAttachment_companyId_fkey" FOREIGN KEY ("companyId") REFERENCES "Company"("id") ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT "DocumentAttachment_storeId_fkey" FOREIGN KEY ("storeId") REFERENCES "Store"("id") ON DELETE SET NULL ON UPDATE CASCADE
)`,
`CREATE INDEX IF NOT EXISTS "DocumentAttachment_companyId_idx" ON "DocumentAttachment"("companyId")`,
`ALTER TABLE "DocumentAttachment" ADD COLUMN IF NOT EXISTS "contentData" TEXT`
];

export async function ensureCommercialSchema(){
  for(const statement of statements){
    await prisma.$executeRawUnsafe(statement);
  }
  console.log("Commercial Database V1 schema bootstrap completed.");
}
