# Master Catalog accepted workbook formats

The importer accepts both the legacy `ΠΡΟΪΟΝΤΑ_IMPORT` template and the current `Προϊόντα` export. Header aliases are normalized server-side so users do not need to rename sheets or columns manually.
